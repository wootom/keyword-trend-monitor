rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // 게시판 파일 업로드 - 누구나 읽기/쓰기 가능
    match /board/{allPaths=**} {
      allow read: if true;
      allow write: if request.resource.size < 10 * 1024 * 1024; // 10MB 제한
    }
  }
}
